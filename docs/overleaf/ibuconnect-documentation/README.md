# iBUConnect Overleaf Document

Upload this folder to Overleaf and set `main.tex` as the main document.

Recommended compiler: **pdfLaTeX**.

Included files:

- `main.tex` — Overleaf-ready LaTeX version of `PROJECT_DOCUMENTATION.md`
- `README.md` — upload notes

The document uses standard Overleaf packages such as `geometry`, `booktabs`, `tabularx`, `longtable`, `tcolorbox`, and `tikz`.
